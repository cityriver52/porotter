{
  "name": "mysns",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A private, SNS-style memo app built entirely with Google Apps Script.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/dev-server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
