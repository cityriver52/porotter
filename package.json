{
  "name": "porotter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Porotter: a private, SNS-style memo app built with Google Apps Script and Google Workspace Studio.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/dev-server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
